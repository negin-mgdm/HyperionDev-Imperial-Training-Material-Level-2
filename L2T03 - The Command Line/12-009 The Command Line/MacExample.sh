#!/bin/bash
# This is an example of a comment terminal
# To run this file, open the terminal cd to the directory
# that contains this file and then type: sh MacExample.sh 
# into the terminal.

clear
mkdir hello_world
cd hello_world
mkdir fullstack
ls